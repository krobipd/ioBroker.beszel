{
  "source": "github:henrygd/beszel",
  "verified": "v0.20.0",
  "_verified": "`verified` means the wire FIELDS were checked at the source and on a real Hub, not that the version number was raised. 0.18.0 audit (2026-09-15): the adapter read `ns`/`nr` (zeroed by the Hub migrator) and six `Max*` peaks that never travel. 2026-09-25 (0.20.0): btrfs pools share `z`/`zfs_pools`, `containers.updatable`, the collections `network_monitors` + `network_monitor_stats`; `dr`/`dw` are zeroed only while `dio == 0` (migration added in 0.19.0); and Hubs from 0.19.0 on are built with encoding/json v2, so an `omitempty` number arrives as 0 instead of being left out (wire capture in Ressourcen/beszel/beszel-0.20.0/wire-captures/). Raising this value means diffing internal/entities/, the hub migrator, the collection snapshot and records_deletion.go against src/lib/types.ts and the metric registry again.",
  "watch": ["internal/entities/", "internal/hub/", "internal/migrations/", "internal/records/", "agent/"],
  "snapshot": "Ressourcen/beszel/beszel-<version>/"
}
